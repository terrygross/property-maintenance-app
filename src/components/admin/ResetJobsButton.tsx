
// This file is being kept empty but not deleted to maintain imports
// It now acts as a placeholder without functionality
import React from "react";
import { Button } from "@/components/ui/button";

// Placeholder component that does nothing
const ResetJobsButton = () => {
  return null;
};

export default ResetJobsButton;
